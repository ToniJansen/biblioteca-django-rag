Django>=4.2,<5.0
django-bootstrap-v5>=1.0
django-tables2>=2.7

# Recomendacao semantica — Fase 1 (HuggingFace, local)
sentence-transformers>=3.0
numpy>=1.24

# Importacao/exportacao de dados (CSV)
pandas>=2.0

# Chat conversacional — Fase 2 (Groq API)
groq>=0.11
python-dotenv>=1.0

# Camada analítica — painel /metricas/ (Plotly)
plotly>=5.20
